# CleanDesk 0.5.0-beta 测试记录

本记录只描述本次归档实际执行过的检查，不代表所有真实用户环境均已覆盖。

## 已执行

- 检查当前 Git 分支为 `feature/0.5-multi-folder`；
- 同步版本号为 `0.5.0-beta`；
- 使用临时配置和临时目录验证空配置启动；
- 使用临时目录验证添加两个监控文件夹、切换当前文件夹；
- 使用临时目录验证重复路径和父子嵌套路径会被拒绝；
- 验证“整理当前文件夹”只处理当前选中文件夹；
- 验证智能整理建议只分析当前选中文件夹；
- 验证“开始自动整理”会扫描并监听两个有效监控文件夹；
- 验证两个监控文件夹中新文件均可被自动整理；
- 验证最近活动显示来源文件夹；
- 验证停止后两个监控文件夹中新文件不会继续自动整理；
- 验证相对目标路径落在各自来源文件夹；
- 验证绝对目标路径不会被拼接到来源文件夹；
- 验证撤销最近整理批次；
- 验证监听状态下撤销不会立刻被再次整理；
- 验证 `1600x900` 双栏布局；
- 验证 `850x700` 单栏布局；
- 执行 `python -m compileall .`；
- 执行 `build_windows.bat`；
- 确认生成 `dist/CleanDesk/CleanDesk.exe`；
- 从 `dist/CleanDesk` 生成 Windows 发布包；
- 生成源码快照。

## 未在本次归档中重新验证

- 未在无 Python 的全新 Windows 机器上做冷启动验证；
- 未逐一验证所有第三方杀毒或受限权限环境；
- 未重新完整验证每一种重名冲突按钮分支。
