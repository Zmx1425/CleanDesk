# CleanDesk 0.5.1-beta 测试记录

本记录只描述本次归档实际执行过的检查，不代表所有真实用户环境均已覆盖。

## 已执行

- 检查当前 Git 分支为 `feature/0.5-multi-folder`；
- 同步版本号为 `0.5.1-beta`；
- 使用临时配置和临时目录验证程序初始化；
- 使用临时目录验证添加两个监控文件夹、切换当前文件夹；
- 验证重复路径和父子嵌套路径会被拒绝；
- 验证“整理当前文件夹”只处理当前选中文件夹；
- 验证智能整理建议只分析当前选中文件夹；
- 验证无监控文件夹时操作按钮会给出提示；
- 验证 Start 会扫描并监听两个有效监控文件夹；
- 验证两个监控文件夹中新文件均可被自动整理；
- 验证最近活动包含来源文件夹；
- 验证 Stop 后不会继续自动整理新文件；
- 验证规则拖拽排序会保存；
- 验证重启服务后规则顺序仍保留；
- 验证规则优先级按拖拽后的顺序生效；
- 验证撤销确认文案显示来源文件夹；
- 验证撤销后文件可移回原位置；
- 验证 `1600x900`、`1200x800`、`850x700` 布局；
- 执行 `python -m compileall .`；
- 执行 `build_windows.bat`；
- 确认生成 `dist/CleanDesk/CleanDesk.exe`；
- 短暂启动打包后的 `CleanDesk.exe` 做冷启动检查；
- 从 `dist/CleanDesk` 生成 Windows 发布包；
- 基于 Git tag 生成源码快照。

## 未在本次归档中重新验证

- 未在无 Python 的全新 Windows 机器上做完整冷启动验证；
- 未重新逐一验证每一种重名冲突按钮分支；
- 未使用真实用户 Downloads / Desktop 做测试。
